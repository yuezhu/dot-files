## Theme: Green on dark grey
#
# Palette:
#   bg        #2a2a2a   Dark grey; also used for inactive pane borders.
#   fg        #b6b6b6   Light grey; primary status text.
#   accent    #77b47a   Green; current-window pill + active pane border.
#   accent-fg #111d12   Very dark green; text on the active pill.
#   bell      #d49837   Orange.
#   bell-fg   #221502   Very dark brown; text on the bell tab.
#   divider   #6e6e6e   Medium grey; right-side field separator.

# Pane borders: active pane matches the current-window pill style.
set-option -g pane-active-border-style "fg=#77b47a"
set-option -g pane-border-style        "fg=#2a2a2a"

# Message bar (command-line input): replaces the entire status bar.
# Command-line input mode (message-command-style) inherits from this style.
set-option -g message-style "bg=#2a2a2a,fg=#b6b6b6"

# Status bar surfaces.
set-option -g status-style                 "bg=#2a2a2a"
set-option -g window-status-current-style  "bg=#77b47a"
set-option -g window-status-activity-style "none"
set-option -g window-status-bell-style     "bg=#d49837"

# Window tab format.
set-option -g window-status-format \
  " #{?window_bell_flag,#[fg=#221502],#[fg=#b6b6b6]}#I:#W#F "
set-option -g window-status-current-format \
  " #[fg=#111d12]#I:#W#F "
set-option -g window-status-separator ""

# Session indicator (left):
#   normal     →  "#S -"
#   prefix     →  "#S P"
#   copy mode  →  "#S C"
# All three states share the bar bg; mode is signaled by the trailing letter.
set-option -g status-left \
  "#[fg=#b6b6b6] #S #{?pane_in_mode,C,#{?client_prefix,P,-}} "


# Extend left block width to fit the session indicator without truncation.
set-option -g status-left-length 60

# Right block: pane count, hostname, and clock.
# ▪ + pane count is shown only when the current window has more than one pane.
# ∣ (U+2223) divider uses the colon-level color.
# date is pinned to America/New_York regardless of the server's system timezone.
set-option -g status-right \
  "#[fg=#b6b6b6]\
 #{?#{e|>:#{window_panes},1},▪ #{window_panes} #[fg=#6e6e6e]∣#[fg=#b6b6b6] ,}\
#h #[fg=#6e6e6e]∣#[fg=#b6b6b6] \
#(TZ='America/New_York' date '+%%a %%b %%d %%l:%%M %%p') "

# Extend right block width to fit the pane count indicator without truncation.
set-option -g status-right-length 60
